{
  "name": "HR-Tech Dashboard (Streamlit)",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "cb_dashboard.py"
      ]
    },
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.analysis.autoImportCompletions": true,
        "python.analysis.typeCheckingMode": "basic",
        "python.languageServer": "Pylance"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "updateContentCommand": "set -e; \
    echo '📦 Updating system...'; \
    sudo apt update && sudo apt upgrade -y; \
    if [ -f packages.txt ]; then sudo xargs apt install -y < packages.txt; fi; \
    echo '📦 Installing Python requirements...'; \
    if [ -f requirements.txt ]; then pip3 install --user -r requirements.txt; fi; \
    pip3 install --user streamlit plotly reportlab; \
    echo '✅ Environment ready.'",
  "postAttachCommand": {
    "server": "streamlit run cb_dashboard.py --server.enableCORS false --server.enableXsrfProtection false --server.headless true --theme.base dark"
  },
  "portsAttributes": {
    "8501": {
      "label": "HR Dashboard",
      "onAutoForward": "openBrowser"
    }
  },
  "mounts": [
    "source=${localWorkspaceFolder}/session_data,target=/workspace/session_data,type=bind"
  ],
  "forwardPorts": [8501],
  "remoteUser": "vscode"
}